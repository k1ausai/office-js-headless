# npm auth token for publishing this package. Create a "Granular Access
# Token" (Automation type, publish access scoped to office-js-headless) at
# https://www.npmjs.com/settings/<you>/tokens. Only needed for a manual local
# publish — normal releases go through CD via a git tag, using the same-named
# NPM_TOKEN GitHub Actions secret instead.
NPM_TOKEN=
